<nav class="navbar navbar-expand-lg navbar-light bg-transparent">
  <a class="navbar-brand" href="#">InnerWork</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to 'Ofertas', job_offers_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to 'Postulaciones', applications_user_path(current_user), class: 'nav-link' %> <!-- Enlace a las postulaciones -->
      </li>
      <% if current_user&.admin? %> <!-- Solo mostrar para el administrador -->
        <li class="nav-item">
          <%= link_to 'Lista Usuarios', users_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>

    <ul class="navbar-nav ml-auto">
     <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <% if user_signed_in? %>
                    Hola <%= current_user.name %>
                <% else %>
                    Inicia o Registrate
                <% end %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <% if user_signed_in? %>
                    <li><%= link_to 'Edit User', edit_user_registration_path, class:'dropdown-item' %></li>
                    <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class:'dropdown-item', method: :delete %></li>
                <% else %>
                    <li><%= link_to 'Iniciar sesión', new_user_session_path, class:'dropdown-item'%></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><%= link_to 'Registrarse', new_user_registration_path, class:'dropdown-item'%></li>
                <% end %>
            </ul>
        </li>
    </ul>
  </div>
</nav>